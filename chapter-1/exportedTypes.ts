export type TMultiplyNumber = (number: number, multiplier: number) => number;

// --
// ignore the next line, it stops TypeScript considering all files in this project as the same block-scope
export {}
